# Project Status

최종 업데이트: 2026-07-10

## 완료

- 회원가입
  - Supabase Auth 기반 회원가입 구현
  - 학교 자유입력 대신 활성 학교 드롭다운에서 포항제철공업고등학교를 선택하고 `POSCO` metadata 저장
  - `public.users` 프로필 연동

- 로그인
  - Supabase Auth 기반 로그인 구현
  - 로그인 후 앱 화면 접근
  - 관리자/교사 role 기반 접근 분리

- 회원정보
  - `/account` 회원정보 페이지 추가
  - 이름, 이메일, 현재 소속학교명, role, 가입일, 최근 수정일 표시
  - Supabase Auth 비밀번호 변경 구현
  - 현재 비밀번호를 `signInWithPassword`로 재확인한 뒤 `updateUser({ password })`로 변경
  - 비밀번호 변경 후 세션 유지 여부 메시지 표시
  - 소속학교 변경 요청 이력 조회와 pending 요청 취소 구현
  - 베타 기간 신규 소속학교 변경 요청 등록 비활성화
  - 승인 전에는 `users.school_id`를 변경하지 않고 기존 학교 데이터 접근 범위 유지

- 학생 CRUD
  - 학생 목록 조회
  - 학생 조회 결과를 학년, 학과, 반, 번호, 이름 순서로 자연 정렬해 표시
  - 학생 관리 화면 최초 진입 시 목록 비표시, 학과/학년/반 선택 후 자동 표시
  - 학과/학년/반 필수 필터와 반 멀티셀렉트, 이름 검색 유지
  - 학생 추가
  - 학생 수정
  - 학생 삭제
  - 학생 관리 화면에서 현재 조회된 학생 기준 선택 삭제와 전체 삭제
  - 학생 삭제 시 연결된 `record_drafts`는 삭제하지 않고 삭제 학생 스냅샷과 함께 archive 처리
  - 학교 ID 기준 데이터 분리

- 학생 엑셀 업로드
  - `.xlsx`, `.xls`, `.csv` 기반 학생 업로드
  - 업로드 후 Supabase 학생 목록 반영

- 학생 템플릿 다운로드
  - 학생 등록용 엑셀 템플릿 다운로드 구현

- 과목 관리
  - `curriculum_subjects` 중심 과목 관리
  - 과목명, 교과유형, 설명, 정렬순서 관리
  - 과목/성취기준 관리 화면을 조회 기반 UI로 개선
  - 과목명 검색, 교과유형 필터, 조회 버튼 기준으로 과목 조회 결과 표시
  - 조회 결과에서 성취기준 개수와 학습모듈 개수 표시
  - 기존 `subjects` 경로는 `/admin/curriculum`으로 리다이렉트

- 성취기준 업로드
  - 성취기준 엑셀 업로드 구현
  - 업로드 미리보기 구현
  - 학습모듈명 컬럼 업로드 구현
  - 핵심키워드 선택 입력 구현
  - 성취기준 업로드 시 엑셀 과목명 기반 `curriculum_subjects` 자동 등록 지원
  - 엑셀 내 동일 과목 반복 행은 과목 1개로만 자동 등록되도록 중복 방지
  - 같은 과목명에 일반교과/NCS교과가 섞인 경우 과목 교과유형 충돌로 미리보기 차단
  - NCS교과 학습모듈명 누락 권장 경고 구현
  - 학교, 과목, 학습모듈, 단원, 성취기준 기준 정확 중복 검사 구현
  - 동일 과목/학습모듈/단원 기준 유사 중복 검사 구현
  - 기존 과목 사용/신규 과목 자동 등록 예정/정확 중복/유사 중복 의심/과목 교과유형 충돌/오류 행 분리
  - 성취기준 템플릿 다운로드 구현
  - 선택 과목 기준 성취기준 조회 구현
  - 선택 과목 성취기준을 학습모듈별 그룹으로 표시
  - 학습모듈 그룹 접기/펼치기와 첫 번째 그룹 기본 펼침 적용

- RAG
  - OpenAI Vector Store 기반 RAG 구조 구현
  - 지식베이스 문서 업로드 API 구현
  - 문서 metadata 저장 구조 구현
  - 생성 근거 검색 흐름 준비
  - 과세특 생성 시 학습모듈 우선 성취기준 후보를 생성 프롬프트에 반영

- 빌드 안정화
  - `lib/generate-api-client.ts` 누락으로 발생하던 빌드 오류 해결
  - 단일 생성, 일괄 생성, Student Record Center 재생성 요청에서 공통 생성 API 클라이언트 사용 흐름 정리

- 과세특 생성
  - `/subject-comment` 단일 생성 화면 구현
  - `/api/generate/subject-comment` 생성 API 구현
  - 개별 작성 기본정보 영역에 공통 `StudentFilter`를 적용해 학과 -> 학년 -> 반 순서로 학생을 조회하고, 여러 반을 선택해도 생성 대상 학생은 1명만 선택하도록 통일
  - Gemini 기반 학생부 문체 생성
  - 과목 선택 시 `curriculum_subjects.subject_type` 기반 일반교과/NCS교과 판정 구현
  - 과목 선택 UI를 공통 `SubjectSelect`로 정리해 전체 과목 options와 선택 value를 분리
  - 일반교과는 학습모듈 선택 비활성화와 안내 문구 표시
  - 과목 변경 시 기존 학습모듈 선택값과 성취기준 preview 상태 초기화
  - NCS교과는 선택 과목의 중복 제거된 전체 학습모듈 목록, 단원 복수 선택, 성취기준 미리보기 제공
  - 학습모듈 선택 시 단원 후보를 select로 제공하고 단원은 선택하지 않아도 생성 가능
  - 단원은 여러 개 선택할 수 있으며 chip으로 표시하고 직접 입력 단원도 추가 가능
  - 단원 선택 UI는 입력값으로 후보 목록을 덮어쓰지 않음
  - `learningModule`, `unit`, `units` payload 전달과 API의 선택 단원 우선 조회, `subject_name + learning_module` fallback 구현
  - 학습모듈, 단원명, 성취기준, 핵심키워드 순서의 관련도 기반 후보 선택
  - 교사 관찰 메모 또는 체크리스트 선택값 중 하나 이상 있으면 생성 가능하도록 입력 조건 완화

- 행특 생성
  - `/behavior-comment` 단일 생성 화면 구현
  - `/api/generate/behavior-comment` 생성 API 구현
  - 개별 작성 기본정보 영역에 공통 `StudentFilter`를 적용해 학과 -> 학년 -> 반 순서로 학생을 조회하고, 여러 반을 선택해도 생성 대상 학생은 1명만 선택하도록 통일
  - 담임 관찰 메모와 학교생활 입력 기반 행동특성 및 종합의견 생성
  - 담임 관찰 메모 또는 체크리스트 선택값 중 하나 이상 있으면 생성 가능하도록 입력 조건 완화

- 과세특 일괄 생성
  - `/bulk-subject-comment` 화면 구현
  - 공통 설정 영역에 과목 유형별 학습모듈 선택 UI 적용
  - 공통 과목 선택 options를 현재 선택값으로 축소하지 않고 전체 과목 목록으로 유지
  - NCS교과에서 선택한 학습모듈을 모든 선택 학생에게 공통 적용
  - 학과, 학년, 반 다중 선택 필터로 동일 교과목을 수강하는 여러 반 학생을 함께 표시
  - 공통 `StudentFilter`로 학과 -> 학년 -> 반 순서의 조회 UX 적용
  - 학과/학년/반 선택 전 학생 입력 테이블 비표시
  - 한 번에 선택 가능한 학생 수를 최대 50명으로 제한
  - 학생별 입력 테이블 상단에 조회 학생 전체 선택과 조회 학생 전체 해제 제공
  - 조회 학생 전체 선택은 필터 결과 또는 기존 선택 합산이 50명을 초과하면 차단하고 안내 표시
  - 선택 학생 수를 `선택 n명 / 최대 50명` 형식으로 표시
  - 학생 다중 선택
  - 학과, 학년, 반 학생 필터의 options와 선택 value를 분리해 관리
  - 학생별 활동유형, 역량키워드, 보완점, 교사 관찰 메모 입력
  - 기존 과세특 생성 API를 학생별로 재사용
  - 동시 호출 수 3개 제한
  - 실패 학생 재생성
  - 학생별 생성 결과 저장

- 행특 일괄 생성
  - `/bulk-behavior-comment` 화면 구현
  - 학과/학년/반 필수 조회와 반 멀티셀렉트 구조 적용
  - 학과/학년/반 선택 전 학생 입력 테이블 비표시
  - 학생별 입력 테이블 상단에 조회 학생 전체 선택과 조회 학생 전체 해제 제공
  - 학생 다중 선택
  - 학과, 학년, 반 학생 필터의 options와 선택 value를 분리해 관리
  - 학생별 학교생활 영역, 생활태도, 협업/관계, 책임감/성실성, 보완점, 담임 관찰 메모 입력
  - 기존 행동특성 생성 API를 학생별로 재사용
  - 동시 호출 수 3개 제한
  - 실패 학생 재생성
  - 학생별 생성 결과 저장
  - `schoolLifeAreas`와 `industrialAttitudes` payload 매핑 정리 완료

- 중복도 분석
  - 일괄 생성 결과 내 유사도 분석 구현
  - 학생 이름 제거 후 텍스트 비교
  - 단어 토큰과 문자 n-gram 기반 cosine similarity 계산
  - 정상/유사/중복 의심 상태 표시
  - 중복 의심 학생 선택 재생성 구현

- 랜덤 분산
  - 과세특 성취기준 후보 관련도 점수 계산
  - 관련도 계산에서 학습모듈을 가장 높은 우선순위로 반영
  - 학습모듈 선택 시 해당 학습모듈 후보 안에서 우선 분산 선택하고, 후보가 없으면 과목 기준으로 fallback
  - 학생 ID, 번호, 이름, 날짜 기반 seed 생성
  - 관련도 높은 후보군 안에서 학생별 성취기준 분산 선택
  - 후보가 3개 이하이면 전체 사용

- 학생부 Lifecycle
  - `record_drafts` lifecycle 구조 구현
  - `ai_content`, `edited_content`, `final_content` 필드 적용
  - `status`, `edited_at`, `finalized_at`, `edited_by` 적용
  - `ai_generated`, `editing`, `saved`, `finalized` 상태 관리
  - `is_current`, `version_no`, `academic_year`, `semester`, `subject_name`, `parent_draft_id` 기반 현재본 구조 추가
  - 현재본 판단 기준을 `user_id + school_id + student_id + mode + subject_name + academic_year + semester`로 정리
  - 같은 기준의 현재본이 있으면 새 AI 생성 시 insert 대신 현재 row update 적용
  - 최종 확정 현재본은 새 AI 생성으로 자동 덮어쓰지 않고 최종 해제 또는 새 결과 사용 흐름으로만 변경
  - 학생 삭제 전 해당 학생의 `record_drafts`를 `is_current = false`로 archive해 `student_id = null` current unique index 충돌 방지

- 자동 저장
  - 교사 수정본 3초 자동 저장 구현
  - 단일 생성과 일괄 생성 결과 편집에 적용
  - `draft_id`가 있으면 해당 row를 직접 update하고, 없을 때만 현재본 기준 fallback 적용

- 최종본
  - 최종 확정 구현
  - 최종 해제 구현
  - 복사/엑셀 다운로드 최종본 선택 규칙 적용
  - 선택 규칙: `final_content -> edited_content -> ai_content -> draft_text`

- Student Record Center
  - `/student-records` 화면 구현
  - 현재 로그인한 교사의 학생별 과세특/행특 현재본 조회
  - 기본 조회를 `is_current = true` 현재본으로 제한
  - 과세특은 과목별 현재본 여러 개, 행특은 학생별 현재본 1개 구조로 표시
  - 학생 목록 필터의 학과, 학년, 반 options와 선택 value를 분리해 관리
  - 학과/학년/반 선택 전 학생 목록 비표시
  - 학생 선택 전 우측 학생부 상세 비표시
  - AI 원본/수정본/최종본 탭
  - 생성 이력 timeline
  - 복사, 재생성, 최종 확정, 최종 해제 액션 연결

- 보안 및 RLS
  - `record_drafts` 조회/수정/삽입 정책을 `user_id = auth.uid()` 기준으로 강화
  - 교사별 학생부 draft 분리 완료
  - 같은 학교 관리자도 기본 정책으로 다른 교사의 학생부 draft를 조회하지 않도록 제한
  - `users` 정책을 본인 프로필 조회, 본인 teacher 프로필 생성, 같은 학교 관리자 조회/role 수정으로 정리
  - 학생, 과목, 성취기준, 학교 설정은 같은 학교 공유 데이터로 유지
  - 과세특/행동특성 생성 API가 클라이언트 `schoolId` 대신 로그인 토큰으로 확인한 학교 ID만 사용하도록 보강
  - 선택 학생이 현재 학교 소속이 아니면 과세특/행동특성 생성 API에서 거부
  - 단일/일괄 생성과 Student Record Center 재생성 요청에 Supabase access token 전달 적용
  - teacher 성취기준 업로드의 과목 자동 등록은 별도 서버 API에서 로그인 토큰과 school_id를 확인한 뒤 처리
  - 일반 과목 관리 화면의 수동 과목 생성/수정/삭제는 admin 권한 유지
  - `school_change_requests` 테이블과 RLS 추가
  - 베타 기간에는 신규 학교 변경 요청 등록을 비활성화하고 기존 요청 조회와 pending 요청 취소만 유지
  - 관리자는 같은 학교 사용자의 pending 학교 변경 요청만 조회
  - 승인/반려는 `approve_school_change_request`, `reject_school_change_request` 보안 함수로 처리
  - 승인 함수는 같은 학교 관리자와 pending 요청만 통과시키고 승인 시에만 `users.school_id` 변경
  - 일반 사용자는 직접 `users.school_id`를 수정할 수 없음

- 관리자 학교 변경 요청
  - `/admin/school-requests` 화면 추가
  - pending 요청 목록, 요청자 이름/이메일, 현재 학교, 요청 학교, 요청 사유 표시
  - 승인 시 요청 학교 ID로 사용자 `school_id` 변경, 요청 상태 `approved`, `reviewed_by`, `reviewed_at` 기록
  - 반려 시 요청 상태 `rejected`, `reviewed_by`, `reviewed_at` 기록

- 엑셀 다운로드
  - 과세특 일괄 생성 결과 다운로드 구현
  - 행특 일괄 생성 결과 다운로드 구현
  - 최종본 선택 규칙 반영
  - 현재본 update 결과를 기준으로 복사와 다운로드 내용 결정
  - 실패 결과 상태 표시

- UI/UX 개선
  - `AppShell` 기반 앱 레이아웃 정리
  - `AppShell`에 회원정보 메뉴 추가
  - 관리자 홈에 학교 변경 요청 관리 진입 카드 추가
  - 대시보드 작업 진입 구조 정리
  - 단일 생성 결과 편집 패널 개선
  - 일괄 생성 입력 테이블과 결과 영역 분리
  - 관리자 설정 화면 구성
  - 과목/성취기준 관리 화면은 최초 진입 시 전체 과목/성취기준 목록을 펼치지 않고 조회 또는 선택 후 표시
  - 선택 과목의 성취기준을 학습모듈 기준 접기/펼치기 그룹으로 표시
  - 과목 선택, 학생 필터, 단원 선택에서 options 배열을 선택값으로 덮어쓰지 않도록 정리
  - 공통 `StudentFilter` 컴포넌트 추가
  - 학생 조회 UX를 학과 -> 학년 -> 반 구조로 변경하고 학생 목록은 학반/번호순으로 정렬
  - 과세특/행동특성 개별작성 학생조회 UX를 일괄작성과 동일한 학과 -> 학년 -> 반 구조로 통일
  - 반 선택을 체크박스 기반 멀티셀렉트 드롭다운과 선택 반 chip으로 변경
  - 반 선택 해제 시 학생 관리, 일괄 생성, 학생부 관리 화면의 학생 목록 또는 입력 테이블을 다시 숨김
  - 성취기준 업로드 영역에 과목 자동 생성, 동일 과목 1회 등록, 교과유형 충돌 차단 안내 추가

- 반응형
  - 데스크톱/모바일 단일 생성 UI 분리
  - 전체 페이지 가로 overflow 개선
  - 넓은 일괄 입력 테이블은 내부 스크롤로 제한
  - 주요 화면 `min-w-0` 구조 보강

- 성능 최적화
  - 일괄 과세특, 일괄 행특, 학생부 관리 화면 1차 성능 최적화 작업 완료

## 진행 중

없음

## 다음 작업

우선순위:

1. 불필요한 버튼 정리
   - 단일 생성, 일괄 생성, 학생부 관리 화면의 버튼 역할을 점검한다.
   - 저장/재생성/확정 계열 액션의 중복 표현을 줄인다.

2. 성능 추가 점검
   - 실제 대량 데이터 기준 렌더링과 유사도 분석 체감 속도를 계측한다.
   - 저장 요청과 재생성 요청의 체감 속도를 추가 점검한다.

3. 담임 Dashboard
   - 담임 기준 반 학생 작성 현황을 모아 보는 화면을 만든다.
   - 미작성/수정 중/최종 확정 상태를 한눈에 확인할 수 있게 한다.

4. RAG 고도화
   - Vector Store 검색 품질을 개선한다.
   - 과목, 학년, 학과, 문서 유형 metadata 필터를 정교화한다.
   - 학습모듈 기반 성취기준 관련도 선택과 RAG 검색 흐름을 통합한다.

## 프로젝트 진행률

| 영역 | 진행률 | 상태 |
| --- | ---: | --- |
| 기능 구현 | 99% | 핵심 생성, 관리, 저장, 다운로드, 계정 관리 흐름 완료 |
| UI | 97% | 주요 화면 구현 완료, 계정/승인 화면 추가, 버튼 정리 필요 |
| AI | 94% | Gemini 생성과 학습모듈 기반 성취기준 반영 완료, RAG 고도화 필요 |
| 학생 관리 | 99% | CRUD, 업로드, 템플릿 완료, 학과/학년/반 조회 UX 적용 |
| 관리자 | 97% | 학과, 과목/성취기준, 체크리스트, 학교 변경 요청 관리 완료 |
| Lifecycle | 99% | AI 원본, 수정본, 최종본 흐름 완료, 교사 소유 RLS 강화 |
| 보안/RLS | 98% | 학생부 개인 소유 정책, 학교 공유 데이터 경계, 학교 변경 승인 정책 적용 |
| 성능 | 90% | 동시 호출 제한 완료, 대량 화면 1차 최적화 완료 |
| 문서 | 96% | README/PROJECT_STATUS 최신화, 운영 문서 추가 여지 있음 |

전체 진행률: 97%

## 변경 이력

- 2026-07-10
  - POSCO 전환 전 운영 기준 `school_id`를 `abcd123`로 확정
  - `abcd1234`의 운영 과목 11건을 `abcd123`로 통합하는 migration 추가
  - `demo-school` 과목 11건은 테스트/샘플 데이터로 분리 유지
  - 운영 기준 `abcd123`의 사용자, 학생, 과목, 성취기준, 학교 설정, 학생부 draft를 `POSCO` 코드로 전환하는 migration 추가
  - `demo-school`, `abcd1234`, `포철공고` 값은 이번 POSCO 전환 대상에서 제외
  - 베타 학교 옵션을 `lib/schools.ts`로 분리하고 포항제철공업고등학교(`POSCO`)를 활성 학교로 설정
  - 회원가입 학교 자유입력을 드롭다운으로 변경하고 가입 payload의 `school_id`를 `POSCO`로 고정
  - 기존 포항제철공업고등학교/포항제철공고/포철공고/demo-school/abcd123/abcd1234 계열 `school_id`를 광범위하게 통합하는 migration 초안은 실행 대상에서 제외
  - 회원정보와 관리자 학교 변경 요청 화면에서 내부 코드 대신 학교명을 표시
  - 행동특성 생성 API도 인증된 사용자 프로필의 학교 ID로 payload를 덮어쓰도록 보강
  - 베타 기간 학교 변경 요청 신규 등록을 비활성화하고 기존 요청 이력과 취소 흐름은 유지

- 2026-07-09
  - `/account` 회원정보 페이지 추가
  - 비밀번호 변경과 현재 비밀번호 재확인 구현
  - `school_change_requests` migration, RLS, 승인/반려 보안 함수 추가
  - 소속학교 변경 요청 생성/취소와 관리자 승인/반려 화면 추가
  - 2026-07-10 기준 베타 기간 신규 요청 등록은 비활성화
  - 일반 사용자의 직접 `school_id` 변경 차단 흐름 정리
  - 행동특성 생성 API에도 현재 학교 학생 소속 검증 추가
  - 학생 조회 결과를 학년/학과/반/번호/이름 기준 자연 정렬로 통일
  - 과세특/행특 일괄 작성 입력 테이블에 조회 학생 전체 선택과 전체 해제 추가
  - 과세특 단원 복수 선택, 직접 추가, 단원 미선택 생성 흐름 적용

- 2026-06-22
  - Supabase Auth와 학생 관리 기반 구조 구현
  - 학생 CRUD와 학교 ID 기준 데이터 분리 적용
  - 관리자 설정 테이블과 role 기반 접근 구조 추가

- 2026-06-24
  - 과목/성취기준 관리 구조 추가
  - 성취기준 엑셀 업로드와 미리보기 구현
  - 정확 중복/유사 중복 검사 구현

- 2026-06-25
  - 과목 관리를 `curriculum_subjects` 중심으로 통합
  - 기존 `subjects` 경로와 데이터는 호환용으로 유지
  - 과세특 생성 시 성취기준 후보 조회 기반 마련

- 2026-06-29
  - 과세특 일괄 생성 화면 구현
  - 행동특성 일괄 생성 화면 구현
  - 일괄 생성 결과 중복도 분석 구현
  - 중복 의심 학생 선택 재생성 구현
  - 생성 결과 엑셀 다운로드 구현

- 2026-06-30
  - `record_drafts` lifecycle 확장
  - AI 원본, 교사 수정본, 최종본 저장 구조 적용
  - 자동 저장, 최종 확정, 최종 해제 구현
  - 학생별 학생부 관리 화면 `/student-records` 구현
  - 생성 이력 timeline 표시 구현

- 2026-07-01
  - 전체 앱 레이아웃과 반응형 구조 개선
  - 일괄 생성 테이블 내부 스크롤 구조 적용

- 2026-07-03
  - 성취기준 업로드 시 엑셀 과목명을 기준으로 `curriculum_subjects` 자동 등록 지원
  - 엑셀 내 동일 과목 중복 자동 등록 방지
  - 과목명 교과유형 충돌 검증과 미리보기 상태 표시 추가
  - 업로드 저장 API에서 과목 자동 upsert 후 성취기준 upsert 처리
  - 행동특성 일괄 생성 payload 매핑 정리
  - 과세특/행특 생성 필수 입력 조건 완화
  - 체크리스트 선택 없이 교사/담임 메모만으로 생성 가능하도록 검증 로직 정리
  - 빈 선택값은 생성 프롬프트와 근거 표시에서 생략하도록 정리
  - README.md와 PROJECT_STATUS.md 전면 최신화

- 2026-07-02
  - `curriculum_standards.learning_module` nullable 컬럼 migration 추가
  - 성취기준 업로드 양식에 학습모듈명 컬럼 추가
  - 핵심키워드 선택 입력 처리와 NCS교과 학습모듈명 권장 경고 추가
  - 성취기준 중복 기준을 학교, 과목, 학습모듈, 단원, 성취기준 조합으로 확장
  - 성취기준 관련도 계산을 학습모듈, 단원명, 성취기준, 핵심키워드 순서로 보강
  - Gemini 프롬프트에 학습모듈을 전달하고 빈 핵심키워드 섹션은 생략하도록 개선
  - 과세특 단일/일괄 생성 화면에 NCS교과 학습모듈 선택, 단원 후보, 성취기준 미리보기 추가
  - 일반교과는 학습모듈 UI를 비활성화하고 기존 과목명 기준 성취기준 조회 유지
  - 과세특 생성 API에서 `learningModule` 전달 시 `subject_name + learning_module` 우선 조회 후 과목 기준 fallback 적용
  - 과세특 단일/일괄 과목 선택을 공통 `SubjectSelect`로 변경해 선택 후에도 전체 과목 목록이 유지되도록 수정
  - 지식베이스 업로드의 과목 선택도 같은 공통 select로 정리
  - NCS교과 학습모듈은 과목 변경 시 초기화하고 새 과목 기준 전체 학습모듈 목록을 다시 로드하도록 점검
  - 일반교과 선택 시 학습모듈 value를 비우고 학습모듈 선택 UI를 비활성화하는 흐름을 점검
  - 단원명 datalist는 학습모듈 선택값 기준 후보만 제공하고 입력값으로 후보 목록을 삭제하지 않도록 점검
  - 과세특/행특 일괄 생성과 Student Record Center의 학생 필터 options가 현재 선택값으로 축소되지 않도록 수정
  - `20260702_secure_record_drafts_rls.sql` migration 추가
  - `record_drafts` select/insert/update RLS를 현재 로그인 교사 `user_id` 기준으로 강화
  - `users` RLS를 본인 프로필, 같은 학교 관리자 사용자/role 관리 범위로 정리
  - 생성 API 요청에 access token을 전달하고 서버에서 학교 ID와 선택 학생 소속을 검증하도록 보강
  - README.md와 PROJECT_STATUS.md에 학교 공유 데이터, 교사 개인 데이터, 관리자 권한, 보안 변경사항 반영

- 2026-07-03
  - 과목/성취기준 관리 화면 최초 진입 시 전체 과목 목록과 최근 성취기준 목록을 바로 펼치지 않도록 조회 기반 UI로 개선
  - 과목명 검색, 교과유형 필터, 조회 버튼 기반 과목 조회 영역 추가
  - 조회 결과에 과목명, 교과유형, 설명, 성취기준 개수, 학습모듈 개수, 보기/수정/삭제 액션 표시
  - 과목 선택 시 해당 과목 성취기준을 학습모듈별로 그룹핑하고 첫 번째 그룹만 기본 펼침 처리
  - 학생 관리 화면 최초 진입 시 학생 목록을 숨기고, 조회 조건 선택 후 자동 표시되도록 UX 개선
  - 일괄 과세특, 일괄 행특, 학생부 관리 화면 성능 최적화 작업 완료
  - `lib/generate-api-client.ts` 누락으로 발생하던 빌드 오류 해결 사항 문서화
  - `record_drafts` RLS 보안 강화 완료 상태 문서화
  - 교사별 학생부 draft 분리 완료 상태 문서화
  - NCS 학습모듈 선택 기능 완료 상태 문서화
  - 과목 선택 `datalist` 버그 수정 완료 상태 문서화

- 2026-07-06
  - `20260706_record_drafts_current_versions.sql` migration 추가
  - `record_drafts`에 현재본 컬럼 `is_current`, `version_no`, `academic_year`, `semester`, `subject_name`, `parent_draft_id` 추가
  - 기존 데이터는 기준별 최신 row만 `is_current = true`, 나머지는 `is_current = false`로 backfill하도록 정리
  - 현재본 기준 unique index와 조회 index 추가
  - `saveRecordDraft`를 같은 현재본 기준 update 우선 구조로 변경
  - 자동저장, 수동저장, 최종확정, 최종해제를 `draft_id` 우선 update로 변경
  - Student Record Center 기본 조회를 `is_current = true`로 제한하고 과세특 과목별 현재본 표시 적용
  - 일괄 과세특/행특 생성 결과가 학생별 현재본을 insert 대신 update하도록 저장 흐름 변경
  - README.md와 PROJECT_STATUS.md에 record_drafts 장기 운영 안정화 내용 반영

- 2026-07-07
  - 공통 `StudentFilter` 컴포넌트 추가
  - 과세특 일괄 작성, 행동특성 일괄 작성, 학생관리, 학생부관리의 학생 조회 UX를 학과 -> 학년 -> 반 구조로 변경
  - 반 선택을 체크박스 멀티셀렉트 드롭다운과 선택 반 chip 구조로 변경
  - 반 선택 해제 시 학생 목록, 학생 입력 테이블, 학생부 상세가 다시 표시되지 않도록 개선
  - 과세특 50명 선택 제한과 기존 생성/lifecycle/중복도/엑셀 다운로드 흐름 유지

- 2026-07-08
  - `20260708_archive_record_drafts_on_student_delete.sql` migration 추가
  - 학생 삭제 전 `record_drafts`에 삭제 학생 id/name/grade/department/class/number 스냅샷과 archive metadata 저장
  - `students` 삭제 trigger에서 해당 학생의 모든 `record_drafts.is_current`를 false로 변경해 FK `ON DELETE SET NULL` 이후 unique index 충돌 방지
  - `deleteStudent`에서 학생 삭제 전 현재 사용자가 소유한 학생부 초안을 먼저 archive 처리하도록 보강
